### 1. 递归终止条件（防止程序崩溃） 🛑

在二叉树遍历中三个遍历函数（`pre_tranverse` 等）没有判断节点是否为空。

- **错误原因**：递归会一直尝试访问 `root->left`，直到访问到无效内存导致 **Segmentation Fault**。
    
- **修正方法**：在函数开始处增加 `if (root == NULL) return;`。
```cpp
#include <iostream>
#include <vector>

using namespace std;

struct TreeNode {
    int data;
    TreeNode* left;
    TreeNode* right;
    // 如果编译器不支持 C++11，请将 nullptr 改为 NULL
    TreeNode(int x) : data(x), left(NULL), right(NULL) {} 
};

// 修正：使用指针的引用 TreeNode* &root，才能修改 main 函数里的 root 指针 [cite: 3, 8]
void insert(TreeNode* &root, int x) {
    if (root == NULL) {
        root = new TreeNode(x);
        return;
    }
    if (x < root->data) {
        insert(root->left, x);
    } else if (x > root->data) {
        insert(root->right, x);
    }
    // 如果 x == root->data，则跳过（题目要求不输出重复元素）
}

// 修正：所有遍历函数都必须检查 root == NULL，否则会发生段错误 [cite: 8]
void pre_traverse(TreeNode* root) {
    if (root == NULL) return;
    cout << root->data << " ";
    pre_traverse(root->left);
    pre_traverse(root->right);
}

void in_traverse(TreeNode* root) {
    if (root == NULL) return;
    in_traverse(root->left);
    cout << root->data << " ";
    in_traverse(root->right);
}

void post_traverse(TreeNode* root) {
    if (root == NULL) return;
    post_traverse(root->left);
    post_traverse(root->right);
    cout << root->data << " ";
}

int main() {
    int n;
    if (!(cin >> n)) return 0; // 读取 n [cite: 5]
    
    TreeNode* root = NULL; 
    for (int i = 0; i < n; i++) {
        int val;
        cin >> val;
        insert(root, val); // 建立二叉排序树 [cite: 3, 8]
    }

    pre_traverse(root);  cout << endl; // 前序遍历 [cite: 3, 8]
    in_traverse(root);   cout << endl; // 中序遍历 [cite: 3, 8]
    post_traverse(root); cout << endl; // 后序遍历 [cite: 3, 8]

    return 0;
}
```
