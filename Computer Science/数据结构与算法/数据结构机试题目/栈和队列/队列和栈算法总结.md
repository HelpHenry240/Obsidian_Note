# 主要算法
## 1 括号匹配
[括号匹配检测-判断括号字符串匹配情况](括号匹配检测-判断括号字符串匹配情况.pdf)
核心思路：**遇到左括号就入栈，遇到右括号就去栈中寻找最近的左括号，看是否匹配**：
```cpp
char LeftMatch(char ch){
	if(ch == ')') return '(';
	else if(ch == ']') return '[';
	else if(ch == '}') return '{'; 
}

bool IsMatch(string&str){
	int size = str.size();
	stack<char> left;
	for(int i=0;i<size;i++){
		if(str[i]=='('||str[i]=='['||str[i]=='{'){
			left.push(str[i]);
		}
		else
		{
			if(!left.empty()&& left.top() == LeftMatch(str[i])){
			left.pop();
		    }
		    else {
			return false;
		    }
			
		}
	}
	return left.empty();
}
```

## 2 表达式求值
[前缀表达式求值-正整数运算符求值](前缀表达式求值-正整数运算符求值.pdf)
[后缀表达式求值-逆波兰式求值](后缀表达式求值-逆波兰式求值.pdf)
### 表达式的分类与核心思想

这三种表达式的本质区别在于**运算符（Operator）相对于**操作数（Operand）**的位置：

|**类型**|**别名**|**举例**|**特点**|
|---|---|---|---|
|**中缀 (Infix)**|算术表示法|`(1 + 2) * 3`|运算符在中间，依靠**括号**和**优先级**确定顺序。|
|**前缀 (Prefix)**|波兰表示法|`* + 1 2 3`|运算符在操作数**前**，无需括号。|
|**后缀 (Postfix)**|逆波兰表示法|`1 2 + 3 *`|运算符在操作数**后**，最适合计算机处理。|
**核心思想：**
前缀和后缀表达式的设计初衷是为了消除歧义和摆脱括号。在计算机处理时，它们可以从一端扫描到另一端，遇到运算符就立即计算，逻辑非常线性。
#### 后缀表达式
**后缀表达式求值算法思想**
想象一下计算机从左向右扫描 `1 2 + 3 *`：

1. 遇到**数字**：先存起来。
    
2. 遇到**运算符**：从存下的数字里取出最后两个进行计算，然后把结果再存回去。
这里有一个关键的编程细节：当你从栈中弹出两个数字（假设为 `a` 和 `b`）进行减法或除法时，顺序非常重要。

如果是后缀表达式 `A B -`：

- 先弹出的是 `B`（右操作数）
    
- 后弹出的是 `A`（左操作数）
    
- 计算的是 $A - B$
```cpp
#include <iostream>
#include <vector>
#include <string>
#include <stack>
#include <sstream> // 用于处理字符串流
//在 C++ 中，处理带空格字符串的利器是 `std::stringstream`。它可以像 `cin` 读取键盘输入一样，一个一个地帮你把数字和符号“倒”出来。

using namespace std;

int evalRPN(string expression) {
    stack<int> s;
    stringstream ss(expression);
    string token;

    while (ss >> token) {
        // 如果 token 是运算符
        if (token == "+" || token == "-" || token == "*" || token == "/") {
            int b = s.top(); s.pop(); // 先弹出的是右操作数
            int a = s.top(); s.pop(); // 后弹出的是左操作数
            
            if (token == "+") s.push(a + b);
            else if (token == "-") s.push(a - b);
            else if (token == "*") s.push(a * b);
            else if (token == "/") s.push(a / b);
        } 
        // 如果 token 是数字
        else {
            s.push(stoi(token)); // 使用 stoi 将字符串转为整数并入栈
        }
    }
    return s.top();
}
```
#### 前缀表达式
倒序转后缀
```cpp
#include <iostream>
#include <vector>
#include <string>
#include <stack>
#include <sstream>
#include <algorithm> // 用于 reverse

using namespace std;

// 检查是否为运算符
bool isOperator(const string& token) {
    return token == "+" || token == "-" || token == "*" || token == "/";
}

int evaluatePrefix(string expression) {
    stack<int> s;
    stringstream ss(expression);
    string token;
    vector<string> tokens;

    // 1. 将表达式拆分并存入 vector
    while (ss >> token) {
        tokens.push_back(token);
    }

    // 2. 从右向左遍历 tokens
    for (int i = tokens.size() - 1; i >= 0; i--) {
        string t = tokens[i];

        if (!isOperator(t)) {
            // 遇到数字：转为整数入栈
            s.push(stoi(t));
        } else {
            // 遇到运算符：弹出两个操作数
            // 注意：由于是从右往左扫，先弹出的是左操作数 a，后弹出的是右操作数 b
            int a = s.top(); s.pop();
            int b = s.top(); s.pop();

            if (t == "+") s.push(a + b);
            else if (t == "-") s.push(a - b);
            else if (t == "*") s.push(a * b);
            else if (t == "/") s.push(a / b);
        }
    }

    // 栈顶即为最终结果
    return s.top();
}

int main() {
    // 示例：+ * 3 4 5  => (3 * 4) + 5 = 17
    string exp = "+ * 3 4 5";
    cout << "Prefix Expression: " << exp << endl;
    cout << "Result: " << evaluatePrefix(exp) << endl;
    return 0;
}
```
#### 中缀表达式
在 **Shunting-Yard 算法**（调度场算法）中，栈的操作遵循以下核心逻辑：

1. **优先级比较**：当我们要把一个新的运算符（如 `+`）压入栈时，如果栈顶已经有一个优先级更高或相等的运算符（如 `*`），说明那个高优先级的运算应该**先被处理**。
    
2. **出栈与输出**：我们会不断地把栈顶那些“更有资格”先算的符号弹出，送到输出队列中，直到栈顶的符号优先级比当前的低，或者栈空了，才把当前的符号压入栈。
    

**为什么必须先弹出 `*`**
因为在后缀表达式中，**运算符的顺序决定了计算顺序**。如果我们先输出了 `*`，那么在求值时，`*` 就会先遇到它的操作数并进行计算。

---

**中缀转后缀的完整流程 🚂**

为了让你更清晰地看到这个过程，我们可以把运算符想象成在路口排队的车辆。

|**扫描到的字符**|**栈（运算符）**|**输出队列（后缀表达式）**|**说明**|
|---|---|---|---|
|`3`|`[]`|`3`|数字直接输出|
|`+`|`[+]`|`3`|栈空，直接入栈|
|`4`|`[+]`|`3 4`|数字直接输出|
|`*`|`[+ *]`|`3 4`|`*` 优先级高，可以压在 `+` 上|
|`2`|`[+ *]`|`3 4 2`|数字直接输出|
|(结束)|`[]`|`3 4 2 * +`|依次弹出栈中剩余符号|

**括号的处理 **

括号就像是一个“隔离舱”。

- 遇到 **左括号 `(`**：无条件入栈。
    
- 遇到 **右括号 `)`**：它会触发一个“清理”动作，不断弹出栈顶符号并输出，直到遇到 `(` 为止（最后把 `(` 丢弃）。
    

---

C++ 实现中的优先级定义

在编写代码时，我们通常会用一个函数来返回运算符的权重：

```cpp
int precedence(string op) {
    if (op == "+" || op == "-") return 1;
    if (op == "*" || op == "/") return 2;
    return 0; // 括号或其他情况
}
```
**完整代码** 
```cpp
#include <iostream>
#include <stack>
#include <string>
#include <vector>
#include <sstream>

using namespace std;

// 定义运算符优先级
int getPrecedence(string op) {
    if (op == "+" || op == "-") return 1;
    if (op == "*" || op == "/") return 2;
    return 0; // 包括左括号 (
}

string infixToPostfix(string expression) {
    stack<string> s;
    stringstream ss(expression);
    string token;
    string result = "";

    while (ss >> token) {
        if (isdigit(token[0])) {
            // 1. 遇到数字，直接输出
            result += token + " ";
        } else if (token == "(") {
            // 2. 遇到左括号，入栈
            s.push(token);
        } else if (token == ")") {
            // 3. 遇到右括号，弹出直到左括号
            while (!s.empty() && s.top() != "(") {
                result += s.top() + " ";
                s.pop();
            }
            s.pop(); // 弹出 "("
        } else {
            // 4. 遇到运算符
            while (!s.empty() && getPrecedence(s.top()) >= getPrecedence(token)) {
                result += s.top() + " ";
                s.pop();
            }
            s.push(token);
        }
    }

    // 将栈中剩余的运算符全部弹出
    while (!s.empty()) {
        result += s.top() + " ";
        s.pop();
    }
    return result;
}
```

# 模拟
## 模拟出栈
[判断堆栈出栈序列是否有效](判断堆栈出栈序列是否有效.pdf)
```cpp
#include <iostream>
#include <vector>
#include <stack>

using namespace std;

int main(){
    int n;
    cin >> n;
    vector<int> nums(n);
    for(int &num : nums){
        cin >> num;
    }
    
    stack<int> s;
    int index = 0;
    
    for(int i = 1; i <= n; i++){
        s.push(i);
        // 关键修改：先检查栈是否为空，再访问栈顶元素
        while(!s.empty() && s.top() == nums[index]){
            s.pop();
            index++;
        }
    }
    
    // 最终的判断应该基于是否能完全匹配
    if(index == n){
        cout << "yes" << endl;
    } else {
        cout << "no" << endl;
    }
    
    return 0;
}
```
